<script>
  import Icon from "@dusk-network/icon";

  const name = "dusk-ticker";
  const size = "xl";
  const variant = "brand";
  const tooltip = "Some helpful information on what this icon represents.";
  const tooltipVisible = "xxl";
</script>

<Icon
  name="{name}"
  size="{size}"
  variant="{variant}"
  tooltip="{tooltip}"
  tooltipVisible="{tooltipVisible}"
/>
